{
  "name": "instancio-js-tests",
  "description": "Tests for the library 'instancio-js, in real production conditions (not inside the instancio-js repository itself, as the configuration would differ)",
  "version": "0.0.0",
  "scripts": {
    "prepare": "ts-patch install",
    "test": "vitest"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.3.0",
    "instancio-js": "^2.0.1",
    "ts-patch": "^4.0.1",
    "typescript": "^6.0.3",
    "vitest": "^3.0.8"
  }
}
