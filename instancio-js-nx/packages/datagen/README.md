# datagen

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build datagen` to build the library.

## Running unit tests

Run `nx test datagen` to execute the unit tests via [Jest](https://jestjs.io).
